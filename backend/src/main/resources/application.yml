spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/qlsk_dd?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD} 
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  profiles:
    active: dev  # <--- Mặc định luôn chạy profile dev khi khởi động

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    user:
      name: user
      password: 123456 # Mật khẩu cố định

# Cấu hình JWT cho xác thực người dùng (Task B1.1-T3)
app:
  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expiration-milliseconds: 86400000 # 24 giờ tính bằng mili-giây